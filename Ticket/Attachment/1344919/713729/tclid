#!/usr/bin/env perl

use strict;
use FindBin '$Bin';
use lib "$Bin/lib";

tclid->run;

package tclid;

use strict;
use base 'CLI::Dispatch';

sub options { qw(
		    help|h|?
		    verbose|v
		    db=s
	       ) }

sub get_command { shift @ARGV || 'Help' }    # no camelization

