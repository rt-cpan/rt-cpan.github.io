#!perl

use strict;
use warnings;

{
	package foo;

	use overload '""' => \&stringify;

	sub new { bless {}, shift @_ };

	sub stringify {
		print "stringify\n"; 'str';
	};

}

use Hash::FieldHash qw[ fieldhash ];

fieldhash my %h;

$h{foo->new} = 1;
