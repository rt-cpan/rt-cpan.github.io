*** /usr/lib/perl5/site_perl/5.6.1/Net/IMAP/Simple.pm	Tue Dec 21 06:23:01 1999
--- Net-IMAP-Simple-0.93/Simple.pm	Tue Jun 11 16:55:00 2002
***************
*** 115,121 ****
      $sh = $self->{sock};
      $id = $self->_nextid();
      
!     print $sh "$id LOGIN $user $pass\r\n";
      $resp = $sh->getline();
  
      if ( $resp =~ /^$id\s+OK/i ) {
--- 115,121 ----
      $sh = $self->{sock};
      $id = $self->_nextid();
      
!     print $sh "$id LOGIN {" . length($user) . "+}\r\n$user {". length($pass) ."+}\r\n$pass\r\n";
      $resp = $sh->getline();
  
      if ( $resp =~ /^$id\s+OK/i ) {
