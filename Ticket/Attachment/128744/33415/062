--- Zip.pm	2005-04-06 14:20:22.346596000 -0700
***************
*** 2685,2690 ****
--- 2685,2696 ----
  	return $status;
  }
  
+ sub endRead    # Archive::Zip::ZipFileMember
+ {
+ 	my $self = shift;
+ 	return $self->Archive::Zip::Member::endRead(@_);
+ }
+ 
  # Because I'm going to delete the file handle, read the local file
  # header if the file handle is seekable. If it isn't, I assume that
  # I've already read the local header.
