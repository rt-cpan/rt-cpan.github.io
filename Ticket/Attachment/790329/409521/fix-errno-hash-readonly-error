--- a/Ident.pm
+++ b/Ident.pm
@@ -204,7 +204,6 @@
     # clear errno in case it contains EINPROGRESS
     if($!{EINPROGRESS}) {
         $! = 0;
-        $!{EINPROGRESS} = 0;
     }
 
     # mark the state of the connection
