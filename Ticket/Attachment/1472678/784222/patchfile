--- a/dbdimp.c	2015-01-27 16:26:55.000000000 +0100
+++ b/dbdimp.c	2015-03-12 12:01:01.650950537 +0100
@@ -26,6 +26,10 @@
 
 #ifdef WIN32
 #define MIN min
+#else
+#ifndef MIN
+#define MIN(a, b)       ((a) < (b) ? (a) : (b))
+#endif
 #endif
 
 #if MYSQL_ASYNC
