--- /rsync_root/perl/lib/Extutils/t/Embed.t	Mon Aug 11 08:18:47 2008
+++ lib/Extutils/t/Embed.t	Fri Dec 26 09:46:24 2008
@@ -37,6 +37,8 @@
 # will be wrong.
 if ($^O eq 'VMS') {
     push(@cmd,$cc,"/Obj=$obj");
+    $inc = VMS::Filespec::vmspath($inc);
+    $lib = VMS::Filespec::vmspath($lib);
     my (@incs) = ($inc);
     my $crazy = ccflags();
     if ($crazy =~ s#/inc[^=/]*=([\w\$\_\-\.\[\]\:]+)##i) {
