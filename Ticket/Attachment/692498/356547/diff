diff --git a/lib/Pod/Extract/URI.pm b/lib/Pod/Extract/URI.pm
index d34a156..517db0e 100644
--- a/lib/Pod/Extract/URI.pm
+++ b/lib/Pod/Extract/URI.pm
@@ -535,7 +535,7 @@ Get/set the use_canonical flag. Takes one optional true/false argument to
 set the use_canonical flag. Defaults to false.
 
 If true, C<Pod::Extract::URI> will store the URIs it finds in the canonical
-form (as returned by C<URI->canonical()>. The original URI and text will
+form (as returned by C<< URI->canonical() >>. The original URI and text will
 still be available via C<uri_details()>.
 
 =cut
