#!/usr/bin/env perl
use 5.10.1;
use strict;
use warnings;

use Tk;
use Tk::Dialog;

my $window = MainWindow->new( -title => 'test' );

my $dialog = $window->Dialog( -title => 'dialog', -text => 'some text' );

MainLoop;
