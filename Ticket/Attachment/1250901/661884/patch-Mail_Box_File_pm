--- Mail/Box/File.pm.orig	Mon Aug 19 02:04:45 2013
+++ Mail/Box/File.pm	Mon Aug 19 02:05:48 2013
@@ -53,6 +53,7 @@
        = $class->folderToFilename
            ( $self->name
            , $self->folderdir 
+           , $self->{MBM_sub_ext} 
            );
 
        if(-e $filename) {;}    # Folder already exists
