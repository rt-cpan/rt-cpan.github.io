89a90,101
> has header => (
>     is => 'rw',
>     isa => 'ArrayRef',
>     trigger => $clearer,
> );
> 
> has footer => (
>     is => 'rw',
>     isa => 'ArrayRef',
>     trigger => $clearer,
> );
> 
126c138
<     foreach my $field ( qw( input_layer syntax escape verbose ) ) {
---
>     foreach my $field ( qw( input_layer syntax escape verbose header footer) ) {
