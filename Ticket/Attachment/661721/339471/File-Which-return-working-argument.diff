--- Which.pm~	2009-09-09 16:19:19.000000000 +0200
+++ Which.pm	2009-09-09 15:27:07.000000000 +0200
@@ -67,10 +67,15 @@
 				last;
 			}
 		}
 	}
 
+	return $exec
+		if(not -d $exec and 
+		   ((!IS_MAC and -x $exec) or
+		    (IS_MAC and -e $exec)));
+
 	my @path = File::Spec->path;
 	if ( IS_DOS or IS_VMS or IS_MAC ) {
 		unshift @path, File::Spec->curdir;
 	}
 
