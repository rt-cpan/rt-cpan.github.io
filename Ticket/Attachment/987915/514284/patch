--- 03lastlog.t.orig	2011-10-14 18:46:07.477180893 -0600
+++ 03lastlog.t	2011-10-14 18:46:25.887207895 -0600
@@ -10,6 +10,6 @@
 
 ok($lp = $ll->lastlog_path(),"lastlog_path()");
 diag( "_PATH_LASTLOG: " . $lp);
-ok($llent = $ll->getlluid(1000),"Get Entry by UID");
-ok($llent = $ll->getllnam('harleypig'),"Get Entry by logname");
+ok($llent = $ll->getlluid($<),"Get Entry by UID");
+ok($llent = $ll->getllnam($ENV{USER}),"Get Entry by logname");
 ok(my $t = $llent->ll_line(),"Get ll_line");
