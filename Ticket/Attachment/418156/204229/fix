#!/bin/sh
for file in META.yml Makefile.PL t/00.t lib/CGI/Auth/Auto.pm
do
    mv $file $file.bkp
    if [ "$file" = "t/00.t" ] ; then
        grep -v PathInfo $file.bkp > $file
    else
        sed s/PathInfo/Scriptpaths/g $file.bkp > $file
    fi
    rm $file.bkp
done

