--- Info.pm.1.02.zlib.noprint	Sun Apr 27 14:55:14 2003
+++ Info.pm.1.02.zlib.noprint.v2.2	Sun Apr 27 14:54:16 2003
@@ -669,6 +669,8 @@
 		read $fh, my($bytes), $size - $hlen;
 
 
+if ($v2->{major_version} >= 3) {
+# ID3V2.2 doesn't have encoding flags (Doesn't even have a standard frame format)
 		if ($eflags && 0x80)
 		{
 		# Zlib tag
@@ -701,6 +703,7 @@
 		next;
 		}
 		
+}
 
 		if (exists $h->{$id}) {
 			if (ref $h->{$id} eq 'ARRAY') {
