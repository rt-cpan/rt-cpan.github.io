#!/usr/bin/perl -w
# script uid-bug
use strict;
use Passwd::Unix;
my $pu = Passwd::Unix->new();
$pu->user( 'testuser', 'secret', 8000, 8000, 'test user,,,', '/home/testuser',
    '/bin/zsh' );
my $id = $pu->uid;
print "id [$id]\n";
