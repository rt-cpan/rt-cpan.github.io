#!/usr/bin/perl

use warnings;
use strict;

BEGIN { $Pod::Usage::Formatter = 'Pod::Text::Termcap'; }
use Pod::Usage;

pod2usage( -noperldoc => 1, -input => \*DATA, -verbose => 2,);


__END__

=pod

=head1 foo

Random I<italic> and B<bold> text;

=cut
