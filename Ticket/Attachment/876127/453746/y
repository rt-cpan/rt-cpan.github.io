#!/usr/bin/perl
use warnings;
use strict;

use Carp;

$SIG{__DIE__} = sub { print "@_: $! (rc=".($!+0).")\n" };

print "--> without \\n\n";
eval { $! = 3; confess "oeps" };
print "\n--> with \\n\n";
eval { $! = 3; confess "oeps\n" };
