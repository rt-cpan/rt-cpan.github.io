Index: Locale/Maketext/Extract.pm
===================================================================
--- Locale/Maketext/Extract.pm	(revision 2218)
+++ Locale/Maketext/Extract.pm	(working copy)
@@ -121,7 +121,7 @@
     my ($self, $file) = @_;
     my $header = '';
 
-    local *LEXICON;
+    local (*LEXICON,$_);
     open LEXICON, $file or die $!;
     while (<LEXICON>) {
         (1 .. /^$/) or last;
