--- ./lib/Schedule/Cron.pm-bak	2010-06-25 13:02:19.626120298 -0600
+++ ./lib/Schedule/Cron.pm	2010-06-25 13:04:01.071232204 -0600
@@ -1107,7 +1107,7 @@
   }
 
 
-  my $args_label = @args ? "with (".join(",",$self->_format_args(@args)).")" : "";
+  my $args_label = @args ? "(".join(",",$self->_format_args(@args)).")" : "";
   $0 = $self->_get_process_prefix()." Dispatched with $args_label"
     unless $cfg->{nofork};
   $log->(0,"Schedule::Cron - Starting job $index $args_label")
