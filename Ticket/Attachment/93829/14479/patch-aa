$NetBSD$

--- Makefile.PL.orig	Mon Dec  1 00:23:29 2003
+++ Makefile.PL
@@ -4,6 +4,8 @@ use ExtUtils::MakeMaker;
 WriteMakefile(
     'NAME'	   => 'Digest',
     'VERSION_FROM' => 'Digest.pm',
+    'PREREQ_PM'	   => { 'MIME::Base64' => 0,
+		      },
     ($] >= 5.008 ? ('INSTALLDIRS'  => 'perl') : ()),
     'dist'         => { COMPRESS => 'gzip -9f', SUFFIX => 'gz', },
 );
