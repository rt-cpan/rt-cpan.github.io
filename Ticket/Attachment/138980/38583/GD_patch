--- GD.pm	2005-08-06 15:39:57.000000000 +0100
+++ GD_corrected.pm	2005-08-08 16:57:19.000000000 +0100
@@ -586,8 +586,8 @@
 
 Example:
 
-	$white = $myImage->colorAllocate(0,0,0); #background color
-	$black = $myImage->colorAllocate(255,255,255);
+	$white = $myImage->colorAllocate(255,255,255); #background color
+	$black = $myImage->colorAllocate(0,0,0);
 	$peachpuff = $myImage->colorAllocate(255,218,185);
 
 =item B<$index = $image-E<gt>colorAllocateAlpha(reg,green,blue,alpha)>
