#!/usr/local/bin/perl
use strict;
use 5.16.0;

use Encode;
say "Encode version: $Encode::VERSION";

my $decoded = decode('MIME-header',<<'EOH');
From: "The quick brown fox runs over the lazy dog
 =?UTF-8?Q?=F0=9F=90=BA?=" <wolfy@example.com>
EOH

say encode_utf8($decoded);

