--- GetLocale.pm	2011-09-20 19:05:39.000000000 +0200
+++ GetLocale.pm.ORIG-1.06	2011-09-20 19:05:44.000000000 +0200
@@ -156,7 +156,7 @@
     $locale ||= $c->config->{I18N}{default_locale};
     
     # Any locale
-    ($locale) = keys %$locale_config unless($locale);
+    ($locale) ||= keys %$locale_config;
     
     if ($c->can('locale')) {
         $c->locale($locale);
