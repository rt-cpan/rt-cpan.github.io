diff Time-Interval-1.1/Interval.pm Time-Interval-1.1-mine/Interval.pm
103c103
< 	if ($p{'String'} != 0){
---
>         if ($p{'Small'} != 0) {
107a108,119
> 				push (@temp, "$time{$_}".substr($_,0,1));
> 			}
> 		}
>                 return join (" ", @temp) || "0s";
> 	}elsif ($p{'String'} != 0){
> 		#return a string?
> 		my @temp = ();
> 		foreach ("days","hours","minutes","seconds"){
> 			if ($time{$_} > 0){
>                           if ($time{$_} == 1) {
> 				push (@temp, "$time{$_} ".substr($_,0,-1));
>                               } else {
108a121
>                               }
111c124
< 		return (join (", ", @temp));
---
> 		return (join (", ", @temp)) || "0 seconds";
