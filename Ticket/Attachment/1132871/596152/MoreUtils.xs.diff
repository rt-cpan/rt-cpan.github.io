--- List-MoreUtils-0.33/MoreUtils.xs	2011-08-04 02:39:36.000000000 -0700
+++ fixed/MoreUtils.xs	2012-10-24 15:48:49.000000000 -0700
@@ -1307,8 +1307,8 @@
 	}
 	if (!tmp[idx])
 	    tmp[idx] = newAV();
-	av_push(tmp[idx], args[i]);
-	SvREFCNT_inc(args[i]);
+
+	av_push(tmp[idx], newSVsv( args[i] ));
     }
     POP_MULTICALL;
 
