--- lib/File/ShareDir/Install.pm	2014-07-08 06:19:35.000000000 +1200
+++ lib/File/ShareDir/Install.pm	2014-08-22 23:23:48.594110287 +1200
@@ -215,7 +215,7 @@
             else {
                 next if $entry =~ /^\./;
             }
-            _scan_share_dir( $files, File::Spec->catdir( $idir, $entry ), $full );
+            _scan_share_dir( $files, File::Spec->catdir( $idir, $entry ), $full, $def );
         }
     }
 }
