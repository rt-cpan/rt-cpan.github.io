--- Pluggable.pm.orig	Sat Jul 09 08:20:38 2005
+++ Pluggable.pm	Tue Jul 26 20:40:17 2005
@@ -417,11 +417,28 @@
               return $opts{'search_path'};
     };
 
+    my $onlysub = sub {
+        my ($self, $only) = @_;
+
+        if (defined $only) {
+            $opts{'only'} = $only;
+        };
+    };
+
+    my $exceptsub = sub {
+        my ($self, $except) = @_;
+
+        if (defined $except) {
+            $opts{'except'} = $except;
+        };
+    };
 
     no strict 'refs';
     no warnings 'redefine';
     *{"$pkg\::$sub"} = $subroutine;
     *{"$pkg\::search_path"} = $searchsub;
+    *{"$pkg\::only"} = $onlysub;
+    *{"$pkg\::except"} = $exceptsub;
 }
 
 
