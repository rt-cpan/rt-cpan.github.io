#!/usr/bin/perl
use warnings;
use strict;

use List::MoreUtils qw(part);

my( $s1, $s2, $s3, $s4 ) = 1..4;

my $i=0;
my @array = part{ int( $i++ / 2 )} ( $s1, $s2, $s3, $s4 );

print "@{$_}\n" foreach @array;

print "\n";
$s2 = 'bogus';

print "@{$_}\n" foreach @array;

