91,101c91,97
<  
< { if( !XML::Twig::_use( "LWP::Simple"))
<     { skip( 1 => "cannot test slurp_uri without LWP::Simple"); }
<   else
<     { use Cwd;
<       my $file = "test_uri";
<       my $uri  = sprintf( "file://%s/%s", getcwd, $file);
<       my $content= "ok";
<       spit( test_uri => $content);
<       is( XML::Twig::_slurp_uri( $uri), $content, "testing _slurp_uri");
<     }
---
> 
> { use Cwd;
>   my $file = "test_uri";
>   my $uri  = sprintf( "file://%s/%s", getcwd, $file);
>   my $content= "ok";
>   spit( test_uri => $content);
>   is( XML::Twig::_slurp_uri( $uri), $content, "testing _slurp_uri");
