--- lib/CGI/Application.pm.orig	2009-05-26 16:11:00.000000000 +0400
+++ lib/CGI/Application.pm	2009-05-26 16:20:09.000000000 +0400
@@ -197,6 +197,7 @@
 
 	# Set up HTTP headers
 	my $headers = $self->_send_headers();
+	utf8::downgrade($headers, 1);
 
 	# Build up total output
 	my $output  = $headers.$body;
