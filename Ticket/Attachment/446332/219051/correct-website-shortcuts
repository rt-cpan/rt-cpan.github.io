Index: lib/Perl/Dist/Inno.pm
===================================================================
--- lib/Perl/Dist/Inno.pm	(revision 3105)
+++ lib/Perl/Dist/Inno.pm	(working copy)
@@ -2237,7 +2237,7 @@
 	# Add the file to the icons section of the inno script
 	$self->add_icon(
 		name     => $website->name,
-		filename => '{app}\\win32' . $website->file,
+		filename => '{app}\\win32\\' . $website->file,
 	);
 
 	return 1;
