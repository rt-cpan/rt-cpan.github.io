--- CPAN.pm_1.76	Thu Jul 31 16:53:06 2003
+++ CPAN.pm	Sun Aug 10 14:08:18 2003
@@ -5808,7 +5808,7 @@
             }
             $file = $ungzf;
         }
-        $system = "$CPAN::Config->{tar} xvf $file";
+        $system = qq($CPAN::Config->{tar} xvf "$file"); # blanks in filenames
         $CPAN::Frontend->myprint(qq{Using Tar:$system:\n});
         if (system($system)==0) {
             $CPAN::Frontend->myprint(qq{Untarred $file successfully\n});
