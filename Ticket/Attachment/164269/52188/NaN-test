--- test.pl	2006/02/12 19:26:13	1.1
+++ test.pl	2006/02/12 19:38:26
@@ -11,7 +11,7 @@
 
 use Math::NumberCruncher;
 use Math::BigFloat;
-$tests = 99;
+$tests = 100;
 print ("Testing Math::NumberCruncher v$Math::NumberCruncher::VERSION\n\n");
 
 print <<END;
@@ -751,6 +751,15 @@
 }
 Testing();
 
+# Check that Predict() can handle BestFit()-generated data
+( $slope, $y_intercept ) = $ref->BestFit( [ 1, 3 ], [ 2, 5 ]);
+$prediction = $ref->Predict( $slope, $y_intercept, 4);
+if ( $prediction !~ /^6\.50000/ ) {
+    print "\nPredict( BestFit( .. )): $prediction\n";
+    $Failed{100} = "Predict(BestFit())";
+}
+Testing();
+
 print ("\n");
 
 @failed = sort { $a <=> $b } keys %Failed;
