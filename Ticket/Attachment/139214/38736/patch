154,155c154,155
<        return "/etc/$_"            if -e "/etc/$filename";
<        return "/usr/local/etc/$_"  if -e "/usr/local/etc/$filename";
---
>        return "/etc/$filename"            if -e "/etc/$filename";
>        return "/usr/local/etc/$filename"  if -e "/usr/local/etc/$filename";
