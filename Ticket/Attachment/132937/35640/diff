--- Email-Store-0.24.orig/lib/Email/Store/Mail.pm	2005-02-06 15:46:12.000000000 -0600
+++ Email-Store-0.24/lib/Email/Store/Mail.pm	2005-06-04 19:29:23.000000000 -0500
@@ -24,8 +24,9 @@
 }
 
 sub store {
-    my ($class, $rfc822) = @_;
-    my $simple = Email::Simple->new($rfc822);
+    my ($class, $message) = @_;
+    my $simple = ref($message) eq "Email::Simple" ? 
+	$message : Email::Simple->new($message);
     my $msgid = $class->fix_msg_id($simple);
 
     my $self;
